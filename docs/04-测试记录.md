# 测试记录

## 2026-08-15 第一版验收

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 空任务提示 | 通过 | 点击“添加任务”后提示填写任务名称。 |
| 新增任务与统计 | 通过 | 新任务立刻显示，全部、待完成、今天到期统计同步更新。 |
| 今天筛选 | 通过 | 设置为今天截止的任务可以在“今天”筛选中看到。 |
| 完成状态 | 通过 | 勾选任务后显示完成状态，刷新页面后仍保持。 |
| 本地保存 | 通过 | 刷新页面后已添加任务仍在。 |
| 删除任务 | 代码已检查 | 页面会先要求确认再删除；自动化测试在浏览器确认框控制上超时，建议首次使用时手动点一次删除确认。 |

## 结论

第一版核心流程可用。当前数据只保存在本浏览器，清除浏览器网站数据会同时清除任务。

## 2026-08-15 备注功能验收

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 新增备注 | 通过 | 可在添加任务时填写最多 200 个字符的备注。 |
| 备注显示与保存 | 通过 | 备注显示在任务下方，刷新页面后仍保留。 |
| 手机布局 | 通过 | 备注输入框和任务备注在窄屏下换行显示，无重叠。 |

## 2026-08-15 T-02 任务分类验收

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 选择与显示分类 | 通过 | 选择“学习”后，任务列表显示“学习”标签。 |
| 刷新后保存 | 通过 | 刷新页面后，“学习”分类仍保留。 |
| 分类可选 | 通过 | 不选择分类时任务可正常创建，列表不显示“未分类”文字。 |
| 旧数据兼容 | 通过 | 缺失、空值或无效分类按无分类显示，原有任务流程不受影响。 |
| 桌面与手机布局 | 通过 | 修复表单网格列数后，桌面控件同排；手机端纵向显示，无重叠。 |

## 测试限制

浏览器自动化在删除确认框的控制上偶发卡顿，因此删除确认框仍建议由项目经理在页面上手动点一次确认。该限制不影响本次分类功能的验收结论。

## 2026-08-16 GitHub Pages 上线冒烟测试

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| HTTPS 页面加载 | 通过 | 公网地址成功加载页面、样式和脚本。 |
| JavaScript 执行 | 通过 | 空任务提交正确显示“请先填写任务名称”。 |
| 浏览器错误 | 通过 | 页面加载与交互未产生控制台错误。 |
| 平板宽度布局 | 修复后复测 | 首次发布发现添加按钮在约 730px 宽度换行，已调整响应式切换点并禁止按钮文字换行。 |

## 2026-08-16 T-02 补充页面复测

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 三种分类 | 通过 | 分别新增“学习”“工作”“生活”任务，列表显示的标签与所选分类一致。 |
| 未选择分类 | 通过 | 新增时保留“未选择”即可提交；列表只显示优先级和日期，不显示分类文字。 |
| 刷新保存 | 通过 | 刷新页面后，三个分类标签与带备注的学习任务均保持正确。 |
| 旧任务兼容 | 通过 | 已有无分类任务正常显示；原有备注“交易任务”仍显示，未出现分类标签。 |
| 完成状态 | 通过 | 对带分类的学习任务完成后再恢复，统计与任务状态同步变化。 |

临时测试任务在测试会话结束时已释放；原有任务未做删除或修改。
