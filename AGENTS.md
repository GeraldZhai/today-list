# 项目协作规则

1. 开始修改前先阅读 `docs/` 中与任务相关的文档，尤其是 `docs/部门协作规范.md`。
2. 第一版只做个人任务管理，不加入登录、多人协作、云端同步或复杂统计。
3. 多步骤功能按“需求 -> 产品设计 -> 开发 -> 测试 -> 项目经理验收 -> 发布运维”交接。小改动可合并需求和产品设计，但必须写明验收标准。
4. 每次功能变更后，更新 `docs/03-任务清单.md`、`docs/04-测试记录.md` 和 `docs/05-变更记录.md`；准备上线时还要更新 `docs/06-部署运维.md`。
5. 保持实现简单：使用原生 HTML、CSS 和 JavaScript，不增加不必要的依赖。
6. 不删除用户任务数据；数据结构变更前必须说明迁移方案。
7. 子 Agent 是按任务临时启动的部门成员。它只能修改被分配的文档或代码，并在完成时填写交接信息。
8. 未经项目经理明确选择平台并授权，不得向外部平台上传代码、创建站点、修改域名或发布版本。
